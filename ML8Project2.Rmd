---
title: "ML8 Fitness Class Analysis"
author: "Ann Crawford"
date: "November 9, 2017"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r readData}
library(caret)
setwd("D:/Data/Coursera/DataScience/8-Machine-Learning")
#trainds <- read.csv(url("https://d396qusza40orc.cloudfront.net/predmachlearn/pml-training.csv"))
#testds <- read.csv(url("https://d396qusza40orc.cloudfront.net/predmachlearn/pml-testing.csv"))
testds <-read.csv("pml-testing.csv",na.strings = c("NA","NaN","","#DIV/0!"))
trainds <-read.csv("pml-training.csv",na.strings = c("NA","NaN","","#DIV/0!"))
#allfactors <- sapply(trainds,is.factor )
```


```{r examineData}
#Does the data contain missing values
sum(is.na(trainds))

dim(trainds)
#19622 obs 159 variables - classe = y
#str(trainds) 
#pricipal comp anal https://www.analyticsvidhya.com/blog/2016/03/practical-guide-principal-component-analysis-python/
#https://machinelearningmastery.com/feature-selection-with-the-caret-r-package/

# check NA's
NAcnt <-sapply(trainds, function(x) sum(is.na(x)))
table(NAcnt)

## 2 groups of predictors
## 67 variables with this count
Nbr2 <- trainds[,which(NAcnt == 19216)]
## 60 varables that have no NA's
NoNA <- trainds[,which(NAcnt == 0)]

## remove varables with No information i.e all NA's
trainds2 <- trainds[,which(NAcnt != nrow(trainds))]

#Subsampling For Class Imbalances - 
table(trainds2$classe)

```
```{r featureSelection}
##https://www.packtpub.com/mapt/book/big_data_and_business_intelligence/9781783984527/5/ch05lvl1sec38/feature-selection-for-svms

## Use Caret built in Recursive Feature Elimination from rf, gbm and K-fold cross validation
# Partion Data into training and test/holdback via resampling

# remove meta data used for data collection and 
x <- subset(trainds2, select=-c(X,user_name, classe, cvtd_timestamp, new_window,amplitude_yaw_belt, amplitude_yaw_dumbbell, amplitude_yaw_forearm ))

## remove highly correlated predictors
predCor <- cor(x, use = "complete.obs")
highlyCorDescr <- findCorrelation(predCor, cutoff = .75)
x <- predictors[,-highlyCorDescr]

library(magrittr)
y <-as.numeric(trainds2$classe)   # change Y to numeric

##unclass(y) %>% as.numeric 

##https://stackoverflow.com/questions/26558631/predict-lm-in-a-loop-warning-prediction-from-a-rank-deficient-fit-may-be-mis
# check for rank deficient coefs, those that are highly correlated
#fit <- lm(y ~ ., data=x)
#predict(fit, trainds2)
#length(fit$coefficients) > fit$rank

subsets <- c(200,205, 208, 222, 230, 150, 148,175,180, 160 )

normalization <- preProcess(x)
x <- predict(normalization, x)
x <- as.data.frame(x)

set.seed(10)

## repeated cross validation 
ctrl <- rfeControl(functions = lmFuncs,
                   method = "repeatedcv",
                   repeats = 5,
                   verbose = FALSE)
# use classifiaction metric
lmProfile <- rfe(x, y,
                 sizes = subsets,metric ="Accuracy",size = 15,maximize = TRUE,
                 rfeControl = ctrl)

lmProfile




```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
